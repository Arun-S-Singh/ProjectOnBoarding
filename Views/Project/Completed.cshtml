@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "List of Brand";
}
@model List<ProjectOnBoarding.Models.Project>
<h1>List of Completed Projects</h1>

<p>
    <a asp-action="Add" class="btn btn-warning" style="color:black">Create New Project</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Description</th>
            <th>Company</th>
            <th>Division</th>
            <th>Brand</th>
            <th>Code</th>
            <th>Completed ?</th>
            
            <th>  </th>
        </tr>
    </thead>
    <tbody>

        @foreach (var project in Model)
        {
            <tr>
                <td>@project.Id</td>
                <td>@project.Name</td>
                <td>@project.Description</td>
                <td>@project.Company</td>
                <td>@project.Division</td>
                <td>@project.Brand</td>
                <td>@project.Code</td>
                <td>@project.IsCompleted</td>
                <td>                    
                    <a class="btn btn-info" asp-controller="Project" asp-action="Details" asp-route-id="@project.Id">Details</a> 
                </td>
            </tr>
        }
    </tbody>

</table>